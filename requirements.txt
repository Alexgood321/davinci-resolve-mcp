# Runtime dependencies for the strict DaVinci Resolve MCP server.
#
# The server imports FastMCP from the official MCP Python SDK and uses AnyIO
# directly for strict LF-delimited stdio transport. Keep these dependencies
# explicit so a clean virtualenv can start the server without relying on
# packages installed as accidental transitive dependencies.
mcp>=1.24.0,<2.0
anyio>=4.8.0,<5.0

# Pure-Python AAF reader used for offline AAF preview. Resolve's native AAF
# import does not depend on it, but the MCP preview/probe path does.
pyaaf2>=1.7.0,<2.0
